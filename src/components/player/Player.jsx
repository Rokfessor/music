import styles from './styles/PlayerStyles.module.css'

const Player = () => {
    return(
        <div className={styles.MainContainer}>
            Player
        </div>
    );
}

export default Player;